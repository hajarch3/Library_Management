@model IEnumerable<Gestion_bibliot.Models.LoanViewModel>

<div class="card">
    <h3>Liste des emprunts</h3>
    <hr />

    <table class="table w-100">
        <thead>
            <tr>
                <th>Étudiant</th>
                <th>Livre</th>
                <th>Auteur</th>
                <th>Exemplaire</th>
                <th>Date d'emprunt</th>
                <th>Date de retour prévue</th>
                <th>Date de retour réelle</th>
                <th>Statut</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        @foreach (var loan in Model)
        {
            <tr>
                <td>@loan.StudentName</td>
                <td>@loan.BookTitle</td>
                <td>@loan.AuthorFullName</td>
                <td>@loan.CopyId</td>
                <td>@loan.LoanDate.ToShortDateString()</td>
                <td>@loan.DueDate.ToShortDateString()</td>
                <td>@(loan.ReturnDate.HasValue ? loan.ReturnDate.Value.ToShortDateString() : "-")</td>
                <td>@loan.Status</td>
                <td>
                    @Html.ActionLink("Modifier", "EditLoan", new { id = loan.Id }, new { @class = "btn btn-sm btn-primary" })
                </td>
            </tr>
        }
        </tbody>
    </table>
</div>
